#include <iostream>
#include <bits/stdc++.h>
#include <string>
#include <vector>
#include <algorithm>
#include <math.h>
using namespace std;

#define ffor(i, a, b) for (int i = a; i < b; i++)
#define bfor(i, a, b) for (int i = a; i > b; i--)
#define sh short
#define us unsigned
#define ll long long
#define ld long double

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int T, n, num1, num2, x;
    char z;
    bool checker;
    string s1, s2 = "", s3 = "hello";
    cin >> T;

    for (int i = 0; i < 12; i++)
    {
        cout << "i am Number " + i << endl;
    }
    return 0;
}